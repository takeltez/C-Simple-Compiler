int main()
{   
    int c = 5 * 3;
    int b;

    if (c == b) {
        char q = '2';
        if (b != 5) {
            char q = 'c';
            int s = b;
        }
    }

    if (b != 5) {
        char q = 'c';
        int s = b;
    }

    if (c > b) {
        char q = 'c';
        int s = 3 * b;
    }

    if (b < 5) {
        char q = 'c';
        int s = b;
    }

    if (c >= b) {
        char q = 'c';
        int s = 3 / b;
    }

    if (b <= 5) {
        char q = 'c';
        int s = b;
    }
   
    return 0;
}